// var input = prompt('masukkan nama:');

const userNames = [
    'yandy',
    'dimas',
    'mita',
];
var newUsername;
if (newUsername = userNames.includes('anjay')) {
    console.log('username sudah digunakan');
} else {
    console.log('username tersedia');
}
